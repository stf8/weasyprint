{
  "name": "weasyprint",
  "version": "1.0.0",
  "description": "weasyprint interface for node",
  "main": "index.js",
  "scripts": {
    "install": "which weasyprint || (virtualenv --system-site-packages ./venv && . ./venv/bin/activate && pip install --upgrade setuptools && pip install --upgrade webencodings && pip install --upgrade WeasyPrint)"
  },
  "author": "Stéphane Mariel",
  "license": "BSD",
  "dependencies": {
    "tmp": "0.0.28"
  }
}
